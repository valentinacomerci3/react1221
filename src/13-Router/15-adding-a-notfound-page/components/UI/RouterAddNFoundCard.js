import classes from './RouterAddNFoundCard.module.css';

const RouterAddNFoundCard = (props) => {
  return <div className={classes.card}>{props.children}</div>;
};

export default RouterAddNFoundCard;
