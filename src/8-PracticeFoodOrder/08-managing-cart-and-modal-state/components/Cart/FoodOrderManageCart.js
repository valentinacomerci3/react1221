import FoodOrderManageCartModal from '../UI/FoodOrderManageCartModal';
import classes from './FoodOrderManageCart.module.css';

const FoodOrderManageCart = (props) => {
  const cartItems = (
    <ul className={classes['cart-items']}>
      {[{ id: 'c1', name: 'Sushi', amount: 2, price: 12.99 }].map((item) => (
        <li>{item.name}</li>
      ))}
    </ul>
  );

  return (
    <FoodOrderManageCartModal onClose={props.onClose}>
      {cartItems}
      <div className={classes.total}>
        <span>Total Amount</span>
        <span>35.62</span>
      </div>
      <div className={classes.actions}>
        <button className={classes['button--alt']} onClick={props.onClose}>
          Close
        </button>
        <button className={classes.button}>Order</button>
      </div>
    </FoodOrderManageCartModal>
  );
};

export default FoodOrderManageCart;
